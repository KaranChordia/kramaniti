export const metadata = {
  title: "Account recovery | Kramaniti Kosh",
  robots: { index: false, follow: false },
};
export default function AccountLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
